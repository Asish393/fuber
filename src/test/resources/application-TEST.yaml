spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:tc:postgresql:11.5.0://localhost/dev_test_fuber

  flyway:
    schemas: public
    locations: filesystem:doc/flyway/migrations,filesystem:doc/flyway/archive,classpath:/sql
fuber-taxi-api:
  url: http://localhost:8912